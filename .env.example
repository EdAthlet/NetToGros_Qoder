# Copy this file to `.env` (never commit `.env`).
# Paste your Neon connection string (same as Netlify DATABASE_URL).
# Prefer the pooled connection string from Neon Connect dialog.

DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require
